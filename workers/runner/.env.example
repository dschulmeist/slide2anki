# Database
DATABASE_URL=postgresql://slide2anki:slide2anki@localhost:5432/slide2anki

# Redis
REDIS_URL=redis://localhost:6379

# MinIO / S3
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=slide2anki
MINIO_SECURE=false

# LLM Configuration
# Preferred: configure provider + model via the web UI (/settings). The worker reads
# persisted settings from Postgres.
#
# Fallback (for running the worker outside Docker):
OPENAI_API_KEY=sk-...
OLLAMA_BASE_URL=http://localhost:11434

# Worker Configuration
WORKER_CONCURRENCY=2
LOG_LEVEL=INFO
